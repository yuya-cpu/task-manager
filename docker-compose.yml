services:
  api:
    build: .
    ports:
      - "8080:8080"
    environment:
      SECRET_KEY: ${SECRET_KEY:-docker-dev-secret-key}
      DB_PATH: /app/data/task-manager.db
      GIN_MODE: release
    volumes:
      - task-data:/app/data
    restart: unless-stopped

volumes:
  task-data:
